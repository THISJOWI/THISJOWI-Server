apiVersion: v1
kind: Service
metadata:
  name: messages
  labels:
    app: messages
spec:
  selector:
    app: messages
  ports:
    - name: http
      protocol: TCP
      port: 80            # port within the cluster
      targetPort: 8086    # container port
  type: ClusterIP
